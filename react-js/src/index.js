import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
//all custom comps go in comps folder expent app.js scince it is root comp
//only do this once for the root components